import React from 'react';
import contact_user from '../style/contact_user.module.scss'

export default function ContactUser(props) {
    return (
        <div className={contact_user.container}>ContactUser</div>
    )
}
